/* let texto = prompt("Ingrese un texto");

 */

let texto = "Tkequisde";

for(let pos in texto) {
    if(pos.indexOf== 'a' | 'e' | 'i' | 'o' | 'u') {
        return pos;
    }else {
        console.log("E");
    }
}
